# 프로젝트 개요

개인 소개 페이지 (자기소개 사이트). 한 페이지로 구성되며 소개 글, 관심사, 링크를 보여준다.

## 스택

- Next.js
- TypeScript
- Tailwind CSS

## 범위

- 단일 페이지(One-page)로 충분함. 별도 라우팅/서브페이지 불필요.
- 구성 요소: 소개 글, 관심사 목록, 링크(예: GitHub, 이메일, 블로그 등) 정도.
- 데이터는 로컬 JSON 파일로 관리 (예: `data/profile.json` 등). DB 연동은 나중에 진행 예정이므로 지금은 손대지 않는다.

## 지금 하지 않는 것

- Vercel 배포 설정
- Supabase 연동 (또는 다른 DB/백엔드 연동)

이 항목들은 추후 별도로 요청 시에만 진행한다.

## 작업 원칙

- 불필요한 추상화, 미래를 대비한 설계 지양. 지금 범위(한 페이지 + 로컬 JSON)에 맞게만 구현.
- 기능 추가 시 위 "지금 하지 않는 것" 항목을 임의로 진행하지 말 것.
